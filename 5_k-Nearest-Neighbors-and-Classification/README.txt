1. Run trynn.py, which will read in iris.test and produce nn.out. The output file contains the nearest neighbor Iris type and the classification of each test point.
1. Run trynn3.py, which will read in iris.test and produce nn3.out. The output file contains the 3 nearest neighbor Iris types and the classification of each test point.


(c) For each row in nn.out, the nearest neighbor based Iris type is the same as the real classification.